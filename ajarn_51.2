#include<stdio.h>

int main()
{
	char a[50];
	int i;
	int up = 0;
	int low = 0;
	gets(a);

	while(a[i]!=0)
	{
		if (a[i] >= 'A' && a[i] <= 'Z')
			up++;
		if (a[i] >= 'a' && a[i] <= 'z')
			low++;	
		
		i++;
	}

	printf("Uppercase : %d\n", up);
	printf("Lowercase : %d", low);
	return 0;
}
